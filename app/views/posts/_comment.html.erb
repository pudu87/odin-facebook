<div>
  <h4>Comment by <%= comment.user.full_name %></h4>
  <p><%= comment.content %></p>
  <% if comment.user == current_user %> 
    <p>
      <%= link_to 'Delete', [comment.post, comment],
        method: :delete, data: { confirm: 'Are you sure?' } %>
    </p>
  <% end %>
</div>